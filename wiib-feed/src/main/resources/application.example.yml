server:
  port: 8081

spring:
  application:
    name: wiib-feed
  threads:
    virtual:
      enabled: true
  # K线落库（KlineStreamCache → common.KlineHistoryStore）
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/wiib?reWriteBatchedInserts=true
    username:
    password:
    hikari:
      pool-name: wiib-feed-hikari
      maximum-pool-size: 6
      minimum-idle: 2
      connection-timeout: 30000
  # 行情总线
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password:
      timeout: 10000

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  metrics:
    tags:
      application: wiib-feed

# Binance行情配置（WS 接入 + REST 回填）
binance:
  ws-url: wss://stream.binance.com:9443/ws
  futures-ws-url: wss://fstream.binance.com/ws
  rest-base-url: https://api.binance.com
  futures-rest-base-url: https://fapi.binance.com
  symbols:
    - BTCUSDT
    - ETHUSDT
    - DOGEUSDT
    - SOLUSDT
    - XRPUSDT
  # bStock（代币化美股，纯现货无合约）：价并入 Spot 流，5m K线走现货端点单独一条连接
  stock-symbols:
    - MUBUSDT
    - SPCXBUSDT
    - SNDKBUSDT
    - CRCLBUSDT
    - QQQBUSDT
    - MSTRBUSDT
    - NVDABUSDT
    - AMDBUSDT
    - TSLABUSDT
    - SOXLBUSDT
  # 大宗商品（TradFi 永续合约，无现货）：黄金/原油，只进合约流
  commodity-symbols:
    - XAUUSDT
    - CLUSDT
  fallback-poll-interval: 5000

# 内部 API 鉴权：/internal/streams 由共享 InternalApiFilter 校验；须与 sim/quant 同值
internal:
  api:
    token: ${INTERNAL_API_TOKEN:}
